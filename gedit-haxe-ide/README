Haxe IDE completion plugin

#----------------------------------------------------------------------------------
Automatic install
#----------------------------------------------------------------------------------
Execute the install.sh as user to have it installed for user only, or as root to install it system wide.


#----------------------------------------------------------------------------------
Manual install
#----------------------------------------------------------------------------------
for root:
copy: haxeide.plugin and haxeide to: /usr/lib/gedit/plugins

for user: 
1)copy: haxeide.plugin and haxeide to: ~/.local/share/gedit/plugins/
2)

for user (as root):
1)copy: org.gnome.gedit.plugins.haxecodecompletion.gschema.xml to: /usr/share/glib-2.0/schemas/
2)run: sudo glib-compile-schemas /usr/share/glib-2.0/schemas/


Relaunch GEdit, go to Edit -> Preferences -> Plugins, and activate Haxe Code Completion.

